<% if obj.errors.any? %>
<div class="alert >
  <h2>The following erros prevented the <%= obj.class.name.downcase %> from being saved.</h2>
  <h4 class="alert-heading">Errors</h4>
  <ul>
  <% obj.errors.full_messages.each do |msg| %>
    <li><%= msg %></li>
  <% end %>
  </ul>
  <% end %> 
</div>